import { makeStyles } from '@mui/styles';

const useStyles = makeStyles((theme)=>({
	container : {
        marginTop: "50px",		
        width: "80%",
    },
  textField : {
    margin: "10px",    
  }
}));



export default useStyles;